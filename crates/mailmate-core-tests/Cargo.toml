[package]
name = "mailmate-core-tests"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

# All test-only: this crate exists so `mailmate-core` itself carries no dev-dependency
# path to `mailmate-test-support` (and thus to any future backend it might re-export).
[dev-dependencies]
mailmate-core = { path = "../mailmate-core" }
mailmate-common = { path = "../mailmate-common" }
mailmate-test-support = { path = "../mailmate-test-support" }
futures.workspace = true
