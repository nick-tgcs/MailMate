[package]
name = "mailmate-storage"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

# EDGE adapter: the ONLY crate that names `rusqlite`. `mailmate-core` never depends on
# this crate, so the no-backend-leakage law (enforced by mailmate-arch-test) is preserved.
[dependencies]
mailmate-common = { path = "../mailmate-common" }
mailmate-ports = { path = "../mailmate-ports" }
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
rusqlite.workspace = true

[dev-dependencies]
futures.workspace = true
time.workspace = true
